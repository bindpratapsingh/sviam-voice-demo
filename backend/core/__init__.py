"""Ported pure modules from SViam (framework-agnostic, unit-tested).

Copied (not imported) from backend/app/services so voice-lab stays isolated from
the SViam app at runtime. These move back into SViam unchanged at merge time.
"""
